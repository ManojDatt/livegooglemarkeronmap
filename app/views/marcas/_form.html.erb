
<section class="content">
  <div class="box-body">


      <%= form_for(marca, :validate => true, html: {class: 'form-horizontal'}) do |f| %>
         <% if marca.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(marca.errors.count, "error") %> prohibited this marca from being saved:</h2>

      <ul>
      <% marca.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
          <div class="row">
            <div class="col-md-12" style="text-align: right;">
              <%= f.submit :class=>"btn btn-md btn-primary", :value=>"Guardar" %>
              <%= link_to 'Cancelar', marcas_path, :class=>"btn btn-md btn-default cancelbtn" %>
            </div>
          
          </div>
        <div class="row">
        <div class="col-md-4">
          <div class="form-group">
             <label>MARCA</label>
            <%= f.text_field :MAR_DESCRIPCION, :class=>"form-control" %>
          </div>
        </div>
  
          <div class="col-md-6">
            &nbsp;
          </div>
        </div>

       
      <% end %>
        <!-- /.row -->
  </div>
</section>
