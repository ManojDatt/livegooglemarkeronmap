
<section class="content">
  <div class="box-body">


      <%= form_for(tarifas_servicio, :validate => true, html: {class: 'form-horizontal'}) do |f| %>
         <% if tarifas_servicio.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tarifas_servicio.errors.count, "error") %> prohibited this tarifas servicio from being saved:</h2>

      <ul>
      <% tarifas_servicio.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
          <div class="row">
            <div class="col-md-12" style="text-align: right;">
              <%= f.submit :class=>"btn btn-md btn-primary", :value=>"Guardar" %>
              <%= link_to 'Cancelar', tarifas_servicios_path, :class=>"btn btn-md btn-default cancelbtn" %>
            </div>
          
          </div>
        <div class="row">
        <div class="col-md-6">
          <div class="form-group">
             <label>DESCRIPCION DEL SERVICIO</label>
            <%= f.text_field :SER_DESCRIPCION, :class=>"form-control" %>
          </div>
        </div>
  
          <div class="col-md-6">
            &nbsp;
          </div>
        </div>
      <div class="row">
        <div class="col-md-6">
          <div class="form-group">
             <label>NOTAS</label>
            <%= f.text_area :SER_NOTAS, :class=>"form-control" %>
          </div>
        </div>
  
          <div class="col-md-6">
            &nbsp;
          </div>
        </div>
       
      <% end %>
        <!-- /.row -->
  </div>
</section>
