<section class="content">
  <div class="box-body">


      <%= form_for(colore, :validate => true, html: {class: 'form-horizontal'}) do |f| %>
         <% if colore.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(colore.errors.count, "error") %> prohibited this color from being saved:</h2>

      <ul>
      <% colore.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
          <div class="row">
            <div class="col-md-12" style="text-align: right;">
             <%= f.submit :class=>"btn btn-md btn-primary", :value=>"Guardar" %>
              <%= link_to 'Cancelar', colores_path, :class=>"btn btn-md btn-default cancelbtn" %>
            </div>
           
          </div>
        <div class="row">
        <div class="col-md-6">
          <div class="form-group">
             <label>Descripcion de Color</label>
            <%= f.text_field :COL_DESCRIPCION, :class=>"form-control" %>
          </div>
        </div>
  
          <div class="col-md-6">
            &nbsp;
          </div>
        </div>

       
      <% end %>
        <!-- /.row -->
  </div>
</section>
